/**
 * <strong>ai.prompt</strong> — versioned prompt templates.
 *
 * <p>Prompts live in code (not scattered as string literals) so they can be
 * reviewed, diffed and tested. Templates use XML tags to delimit role, context,
 * task, rules and few-shot examples, which makes the structure explicit for the
 * model and easy to maintain for humans.
 */
package com.renansiqueira.claudelab.ai.prompt;
