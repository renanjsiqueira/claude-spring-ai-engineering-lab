/**
 * <strong>api</strong> — HTTP boundary (controllers, request/response DTOs).
 *
 * <p>Translates HTTP into calls on the {@code application} layer and back.
 * Keeps no business logic of its own.
 */
package com.renansiqueira.claudelab.api;
