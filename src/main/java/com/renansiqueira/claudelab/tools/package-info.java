/**
 * <strong>tools</strong> — tool/function definitions Claude can call.
 *
 * <p>Each tool exposes a typed input schema and an implementation the model can
 * invoke during tool use. Added in the tool-use phase.
 */
package com.renansiqueira.claudelab.tools;
