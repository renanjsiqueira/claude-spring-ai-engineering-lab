/**
 * <strong>persistence</strong> — JPA entities and Spring Data repositories.
 *
 * <p>Kept separate from {@code domain} (which stays framework-free). Entities map
 * to the Flyway-managed schema; the application layer maps them to/from DTOs.
 */
package com.renansiqueira.claudelab.persistence;
